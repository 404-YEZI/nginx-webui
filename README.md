# Nginx小白管理工具（nginx-webui）
[![GitHub](https://img.shields.io/github/stars/404-YEZI/nginx-webui?style=social)](https://github.com/404-YEZI/nginx-webui)  
一款面向**零基础用户**的Nginx可视化管理工具，基于Python Flask + 原生JS + TailwindCSS开发，轻量化、无复杂依赖，支持一键部署，涵盖Nginx站点管理、反向代理、SSL证书、日志查看、服务操作等核心功能，彻底告别命令行配置Nginx的繁琐。

## 🌟 核心优势
- 🚀 **零基础友好**：全程可视化操作，无需懂Nginx配置语法，小白也能快速上手；
- 🛠️ **功能全覆盖**：站点/代理/SSL/日志/服务/备份一站式管理；
- ⚡ **轻量化设计**：仅依赖Python Flask，无数据库（配置直接落地Nginx原生文件），资源占用极低；
- 🔒 **安全防护**：内置高危路径/命令过滤，避免误操作导致服务器崩溃；
- 📱 **响应式布局**：适配PC/手机端，随时随地管理Nginx；
- 📦 **一键部署**：提供通用一键安装脚本，Debian/Ubuntu/CentOS全兼容。

## 📋 适配环境
- 操作系统：CentOS 7+/Ubuntu 16.04+/Debian 9+（其他Linux发行版需手动适配依赖）；
- 依赖要求：Python 3.6+、Nginx（脚本会自动检测/安装）；
- 权限要求：必须以`root`用户运行（涉及Nginx配置、系统服务操作）。

## 🚀 快速安装部署
### 方式1：一键脚本安装（推荐）
一行命令完成所有部署（自动安装依赖、配置服务、启动程序）：
```bash
# 下载并执行一键安装脚本
wget -O nginx_web_tool_menu.sh https://raw.githubusercontent.com/404-YEZI/nginx-webui/main/nginx_web_tool_menu.sh && chmod +x nginx_web_tool_menu.sh && ./nginx_web_tool_menu.sh
```
执行后进入交互式菜单，选择「1. 安装nginx-webui」，脚本会自动完成：
1. 检测系统依赖（Python 3、pip、Nginx）；
2. 下载项目源码到`/usr/local/nginx-webui`；
3. 配置systemd服务（开机自启）；
4. 启动服务并监听8888端口。

### 方式2：手动部署（适合自定义配置）
#### 1. 克隆代码仓库
```bash
git clone https://github.com/404-YEZI/nginx-webui.git
cd nginx-webui
```

#### 2. 安装依赖
```bash
# CentOS
yum install -y python3 python3-pip nginx
# Ubuntu/Debian
apt update && apt install -y python3 python3-pip nginx

# 安装Python依赖
pip3 install flask
```

#### 3. 启动服务
```bash
# 直接启动（前台运行，适合调试）
python3 run.py

# 或配置systemd服务（后台运行+开机自启）
cat > /etc/systemd/system/nginx-webui.service << EOF
[Unit]
Description=Nginx Web UI Service
After=network.target nginx.service

[Service]
Type=simple
User=root
WorkingDirectory=/usr/local/nginx-webui  # 替换为你的项目路径
ExecStart=/usr/bin/python3 run.py
Restart=on-failure

[Install]
WantedBy=multi-user.target
EOF

# 启动并设置开机自启
systemctl daemon-reload
systemctl start nginx-webui
systemctl enable nginx-webui
```

#### 4. 访问工具
浏览器访问 `http://服务器IP:8888` 即可进入管理界面（默认无密码，生产环境建议配合Nginx添加基础认证）。

## 📂 项目目录结构详解
```
nginx-webui/
├── api/                     # 后端Flask接口核心目录
│   └── app.py               # Flask主程序（实现所有功能接口：状态查询/站点管理/文件操作等）
├── scripts/                 # 辅助脚本（被后端调用，实现自动化操作）
│   ├── certbot_ssl.sh       # Let's Encrypt SSL证书一键申请/续期（自动适配域名）
│   ├── backup_config.sh     # Nginx配置自动备份（按日期归档，存到data/backup）
│   └── restore_config.sh    # 配置一键回滚（选择备份文件快速恢复）
├── static/                  # 前端静态资源（极简设计，仅核心交互JS）
│   └── main.js              # 前端所有交互逻辑（菜单切换/按钮点击/表单提交等）
├── templates/               # 前端HTML模板（卡片式布局，适配移动端）
│   ├── index.html           # 首页（集成Nginx状态监控+功能导航）
│   ├── site.html            # 站点管理（可视化增/删/改/查站点，自动补全Nginx配置）
│   ├── proxy.html           # 反向代理配置（独立配置文件，参数自动补全，无需手写proxy_pass）
│   ├── ssl.html             # SSL证书管理（一键申请Let's Encrypt/手动上传证书/自动续期）
│   ├── file.html            # 文件管理器（仅允许操作非高危Nginx配置文件，支持上传/下载/删除）
│   ├── backup.html          # 配置备份/回滚（查看备份列表，一键恢复任意版本配置）
│   ├── service.html         # Nginx服务操作（校验配置/重载/重启/停止，实时显示运行状态）
│   └── log.html             # 日志管理（查看/筛选/下载访问日志/错误日志，支持关键词过滤）
├── data/                    # 数据存储目录（自动生成，无需手动修改）
│   └── .gitkeep             # 空文件让Git识别目录（实际存储备份/证书/日志缓存）
├── run.py                   # 项目一键启动脚本（封装Flask启动逻辑，简化用户操作）
├── install.sh               # 一键安装部署脚本（兼容多系统，自动处理依赖和服务配置）
├── README.md                # 项目说明文档（零基础安装/使用指南）
├── .gitignore               # Git忽略文件（避免上传data/、依赖包等无用数据）
├── LICENSE                  # 开源协议（MIT）
└── nginx_web_tool_menu.sh   # 交互式一键管理脚本（安装/卸载/启动/停止/配置修改）
```

## 📌 核心功能模块详解
### 1. 首页（index.html）
- 集成Nginx实时状态监控（运行状态、PID、版本、监听端口）；
- 快速导航到所有功能模块，无需手动切换URL；
- 适配移动端，菜单自动折叠为图标模式。

### 2. 站点管理（site.html）
- **可视化配置**：无需手写Nginx配置，填写网站名称、域名、端口、根目录即可生成最优配置；
- **自动校验**：添加/修改站点后自动执行`nginx -t`校验配置，错误时自动回滚并提示原因；
- **一键生效**：配置完成后自动重载Nginx，无需手动执行命令；
- **批量管理**：支持查看/删除已配置站点，配置文件存储在`/etc/nginx/conf.d/`（原生Nginx路径，兼容手动修改）。

### 3. 反向代理（proxy.html）
- **极简配置**：仅需填写代理名称、监听端口、目标地址（如`http://127.0.0.1:8080`），自动补全`proxy_pass`、`proxy_set_header`等核心参数；
- **独立配置文件**：每个代理生成独立配置文件，避免单文件过大导致维护困难；
- **参数优化**：自动添加超时时间、缓存控制等最优参数，适配大多数场景。

### 4. SSL证书（ssl.html）
- **一键申请**：输入域名即可自动调用`certbot`申请Let's Encrypt免费SSL证书，自动配置HTTPS；
- **手动上传**：支持上传已有SSL证书（crt/key文件），自动生成HTTPS配置；
- **自动续期**：内置定时续期逻辑，避免证书过期导致网站无法访问；
- **配置检测**：自动校验证书路径和配置，确保HTTPS生效。

### 5. 文件管理（file.html）
- **安全访问**：过滤高危路径（`/etc/nginx/nginx.conf`、`/root`、`/boot`等），仅允许操作`conf.d/`等安全目录；
- **文件操作**：支持查看/上传/下载/删除Nginx配置文件，实时显示文件大小、修改时间；
- **语法高亮**：配置文件查看时自动高亮，方便排查语法错误。

### 6. 配置备份（backup.html）
- **自动备份**：每天自动备份Nginx配置目录（`/etc/nginx`），按日期命名（如`nginx_backup_20240501.tar.gz`）；
- **手动备份**：支持手动触发备份，自定义备份名称；
- **一键回滚**：选择备份文件即可快速恢复配置，恢复前自动校验，避免配置错误。

### 7. 服务操作（service.html）
- **状态监控**：实时显示Nginx运行状态（运行/停止）、PID、版本、监听端口；
- **快捷操作**：
  - 校验配置：执行`nginx -t`并返回中文提示；
  - 重载配置：执行`nginx -s reload`（不中断服务，推荐配置修改后使用）；
  - 重启服务：执行`systemctl restart nginx`（仅重载无效时使用）；
  - 停止/启动：执行`systemctl stop/start nginx`；
- **操作提示**：明确标注各操作风险（如停止服务会导致网站无法访问）。

### 8. 日志管理（log.html）
- **日志筛选**：支持切换访问日志（`access.log`）/错误日志（`error.log`），自定义显示最后N行（10-1000行）；
- **关键词过滤**：输入关键词（如404、IP、域名）快速筛选日志，定位问题；
- **日志下载**：一键下载筛选后的日志文件，方便本地分析；
- **自适应显示**：日志内容使用等宽字体，支持换行/滚动，移动端适配。

## 🔒 安全保障
1. **高危路径过滤**：禁止访问/修改`/etc/nginx/nginx.conf`核心配置、`/root`、`/boot`等高危目录，避免误操作导致服务器崩溃；
2. **高危命令过滤**：拦截`rm -rf /`、`mv /`、`chmod 777 /`等危险命令，防止恶意/误操作；
3. **配置校验**：所有配置修改后自动执行`nginx -t`，错误时自动回滚，避免Nginx无法启动；
4. **权限控制**：仅允许`root`用户运行，避免普通用户权限不足导致操作失败。

## ❓ 常见问题与排查
### 1. 访问工具时页面无法加载
- 检查服务器防火墙/安全组是否放行8888端口；
- 执行`systemctl status nginx-webui`查看服务是否启动；
- 检查Python依赖是否安装：`pip3 list | grep flask`。

### 2. Nginx操作提示“权限不足”
- 确保以`root`用户运行脚本/服务；
- 检查Nginx配置文件权限：`ls -l /etc/nginx/conf.d/`。

### 3. 配置校验失败
- 查看错误提示（如端口被占用、域名格式错误、配置语法错误）；
- 手动执行`nginx -t`查看详细错误信息。

### 4. SSL证书申请失败
- 确保域名已解析到服务器，且80端口未被占用；
- 检查服务器是否能访问外网（Let's Encrypt需要联网验证）；
- 执行`scripts/certbot_ssl.sh 你的域名`手动运行脚本，查看错误日志。

## 📄 开源协议
本项目基于 **MIT 协议** 开源，你可以自由修改、分发、商用，但需保留原作者版权信息。

## 🤝 贡献指南
欢迎提交Issue/PR改进项目：
1. Fork本仓库；
2. 创建功能分支（`git checkout -b feature/xxx`）；
3. 提交代码（`git commit -m 'add: xxx功能'`）；
4. 推送分支（`git push origin feature/xxx`）；
5. 提交Pull Request。

## 📞 问题反馈
如果使用过程中遇到问题，可通过以下方式反馈：
- GitHub Issues：https://github.com/404-YEZI/nginx-webui/issues
- 仓库讨论区：https://github.com/404-YEZI/nginx-webui/discussions
